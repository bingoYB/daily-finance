{
  "name": "daily-finance",
  "version": "1.0.0",
  "description": "每日发布的财务静态页面。",
  "main": "script.js",
  "scripts": {
    "start": "browser-sync start --server --files '*.html, *.css, *.js'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "browser-sync": "^3.0.4"
  }
}
